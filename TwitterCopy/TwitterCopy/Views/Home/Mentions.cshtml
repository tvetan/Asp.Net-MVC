@{
    ViewBag.Title = "Mentions";
}

@section  MiniProfile {
    <ul class="nav">
        <li>Interactions</li>
        <li>Mentions</li>
    </ul>
}

@section RandomUsers {
    <div class="row">
        <div class="row module-header">
            <h3>Who to follow</h3>
            <small>. </small>
            @Ajax.ActionLink("Refresh", "RandomUsers", "user", new AjaxOptions() { })
            <small>. </small>
            @Html.ActionLink("View all", "All", "user")
        </div>
        <div class="random-followers">
            @Html.Action("RandomUsers", "user")
        </div>
    </div>
}
@*</div>*@

@*<div class="col-md-4">*@
@section ListItems {
   <h3>Mentions</h3>
    <p>Tweets that mention you, or are in reply to you, appear here. </p>
}